{
  "name": "fs-live-viz",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "serve": "python3 -m http.server 4173",
    "framer:deploy": "node scripts/deploy-framer.mjs",
    "framer:publish": "node scripts/deploy-framer.mjs --publish"
  },
  "devDependencies": {
    "framer-api": "^0.1.12"
  }
}
